# Secrets and environment-specific identifiers — never commit
.env
.env.local

# Lambda build artifacts
*.zip

# Python
__pycache__/
*.py[cod]

# Editor / OS
.DS_Store

# Deployed v3 snapshot keeps the real instance ID for rollback — local only
backup/lambda_function.v3.py

# Claude Code session data (worktrees, scratch) — local only
.claude/
